id = "INT-001"
milestone = "v0.1"
title = "Prove v0.1 end-to-end architecture and verification runbook"
goal = "Compose the v0.1 modules into a deterministic end-to-end fixture path and one development-host local network proof without expanding feature scope."
depends_on = ["NET-001", "CFG-001", "CAP-001", "STORE-001", "WASM-001", "UI-001"]
allowed_paths = ["tests/integration/**", "tests/e2e/**", "docs/milestones/v0.1.md", "scripts/**", ".github/workflows/**"]
forbidden_paths = ["PRODUCT.md", "ARCHITECTURE.md", "docs/adr/**", "docs/contracts/**"]
acceptance = ["python scripts/validate_tasks.py", "python scripts/quality_gate.py", "cargo fmt --all -- --check", "cargo clippy --workspace --all-targets -- -D warnings", "cargo test --workspace", "pnpm --dir apps/desktop lint", "pnpm --dir apps/desktop test"]
definition_of_done = ["Deterministic fixture travels Capture -> NormalizedFlow -> Storage -> Analyzer -> SemanticEvent", "Verifier runbook works from clean checkout", "Real local HTTP/TLS proof is documented with unsupported-host limitations explicit", "No v0.2+ feature is pulled into v0.1"]
