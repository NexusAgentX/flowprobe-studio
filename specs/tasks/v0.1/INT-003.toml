id = "INT-003"
milestone = "v0.1"
title = "Re-verify v0.1 after task-scope correction"
goal = "Integrate the task-scope correction into the clean-checkout release gate and independently prove that every v0.1 outcome is complete while preserving the historic nonconformance record."
depends_on = ["SCOPE-001", "INT-002"]
allowed_paths = ["scripts/check_v0_1_integration.py", ".github/workflows/contract.yml", "docs/milestones/v0.1.md"]
forbidden_paths = ["PRODUCT.md", "ARCHITECTURE.md", "ROADMAP.md", "docs/adr/**", "docs/contracts/**", "specs/tasks/**", "specs/nonconformance/**", "apps/**", "crates/**", "runtime/**", "plugins/**", "tests/**", "Cargo.toml", "Cargo.lock", "pnpm-lock.yaml", "mise.toml"]
acceptance = ["python -B scripts/validate_tasks.py", "python -B -m unittest discover -s scripts/tests -p 'test_quality_gate.py'", "python -B -m unittest discover -s scripts/tests -p 'test_task_scope.py'", "python -B scripts/quality_gate.py", "python -B scripts/check_task_scope.py audit --ledger specs/nonconformance/v0.1/task-scope.toml", "cargo fmt --all -- --check", "cargo clippy --workspace --all-targets --locked -- -D warnings", "cargo test --workspace --locked", "cargo test --manifest-path tests/integration/v0_1/Cargo.toml --locked --all-targets", "cargo clippy --manifest-path tests/integration/v0_1/Cargo.toml --locked --all-targets -- -D warnings", "RUSTDOCFLAGS=-Dwarnings cargo doc --workspace --locked --no-deps", "RUSTDOCFLAGS=-Dwarnings cargo doc --manifest-path tests/integration/v0_1/Cargo.toml --locked --no-deps", "python -B scripts/check_v0_1_integration.py", "pnpm --dir apps/desktop lint", "pnpm --dir apps/desktop test", "pnpm --dir apps/desktop build", "mise run check", "git diff --check"]
definition_of_done = ["The clean-checkout integration gate explicitly runs the task-scope self-tests and historic ledger audit in addition to every existing v0.1 proof", "A fresh clone verifies the ledger against the real first-parent history and confirms that the original thirteen task contracts and the milestone exit criterion were not rewritten", "Every executable acceptance command and concrete Definition-of-Done outcome for the original thirteen v0.1 tasks is rechecked against the current tree", "On a supported verifier host the pinned sing-box missing-environment failure, executable-SHA mutation failure, positive HTTP and HTTPS proof, and cleanup audit are repeated", "Root and isolated Rust checks, desktop checks, contract gates, CI, and the full mise check pass from the exact final revision", "No tracked or untracked checkout change, proof process, temporary proof directory, Python bytecode cache, or managed runtime state remains", "An independent verifier reports P0, P1, and P2 all zero and describes the eight historic pull requests as nonconforming-at-merge but corrected-and-closed by SCOPE-001", "The v0.1 milestone is not declared complete until the exact merged main revision passes this task and its independent release audit"]
