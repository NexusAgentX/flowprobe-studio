id = "GOV-003"
milestone = "v0.1"
title = "Remove mandatory main approvals and re-prove governance"
goal = "Reconcile GOV-001 and its operator documentation with the user's explicit decision to remove mandatory human approval, while retaining the exact trusted status checks, permissions, protected-main restrictions, negative canaries, and independent technical review."
depends_on = ["PLAN-005", "GOV-002"]
allowed_paths = ["specs/tasks/v0.1/GOV-001.toml", "docs/development/TASK_CONTRACT.md"]
forbidden_paths = ["AGENTS.md", "PRODUCT.md", "ARCHITECTURE.md", "ROADMAP.md", "docs/adr/**", "docs/contracts/**", "docs/milestones/**", "specs/tasks/README.md", "specs/tasks/v0.1/PLAN-005.toml", "specs/tasks/v0.1/GOV-002.toml", "specs/tasks/v0.1/GOV-003.toml", "specs/nonconformance/**", "apps/**", "crates/**", "runtime/**", "plugins/**", "tests/**", "scripts/**", ".github/**", "Cargo.toml", "Cargo.lock", "package.json", "pnpm-lock.yaml", "mise.toml"]
acceptance = [
    "python -B scripts/validate_tasks.py",
    "python -B -m unittest discover -s scripts/tests -p 'test_task_scope.py'",
    "python -B scripts/quality_gate.py",
    "python -B scripts/check_task_scope.py audit --ledger specs/nonconformance/v0.1/task-scope.toml",
    "python -B scripts/check_task_scope.py check --task GOV-003 --base-ref origin/main --head-ref HEAD",
    '''python -B -c 'import hashlib, os, subprocess, sys; env=os.environ | {"GIT_NO_REPLACE_OBJECTS":"1","LC_ALL":"C"}; run=lambda *a: subprocess.check_output(["git","--no-replace-objects",*a],env=env); bases=run("merge-base","--all","origin/main","HEAD").decode().splitlines(); len(bases)==1 or sys.exit("expected exactly one merge base"); base=bases[0]; expected={"specs/tasks/v0.1/GOV-001.toml":("e114ee0d1b610bbc0c99f84338cdc8703d550bc875202d0c107632d6b02c05b3","5e2072785b8aecafaa6a8472487ab1d4cc70e22f1ace38b2d0ea076d742ea9a8"),"docs/development/TASK_CONTRACT.md":("fe8e68dad0642b30458829b3b4f31a366734acd656fa351cb1f9b40ac68b08b4","691d1789fc570fc2b7591c31edb8ac14bf967d92ad3b0d56229fb7cc8efde8bb")}; changed={p.decode() for p in run("diff","--no-ext-diff","--no-textconv","--no-renames","--name-only","-z",base,"HEAD").split(b"\0") if p}; changed==set(expected) or sys.exit("unexpected changed path set"); mode=lambda r,p: run("ls-tree",r,"--",p).split(None,2)[:2]==[b"100644",b"blob"]; all(mode(r,p) for r in (base,"HEAD") for p in expected) or sys.exit("expected regular mode-100644 blobs"); digest=lambda r,p: hashlib.sha256(run("cat-file","blob",r+":"+p)).hexdigest(); all(digest(base,p)==h[0] and digest("HEAD",p)==h[1] for p,h in expected.items()) or sys.exit("unauthorized file bytes")' ''',
    "python -B scripts/check_v0_1_integration.py",
    "mise run check",
    "git diff --check origin/main...HEAD",
    '''set -o pipefail; gh api -H 'X-GitHub-Api-Version: 2026-03-10' repos/NexusAgentX/flowprobe-studio/branches/main/protection --jq '(.required_status_checks.strict == true) and ([.required_status_checks.checks[] | {context, app_id}] | sort_by(.context) == ([{context: "contracts", app_id: 15368}, {context: "Rust workspace", app_id: 15368}, {context: "Desktop renderer", app_id: 15368}, {context: "Task scope (trusted base)", app_id: 15368}] | sort_by(.context))) and (.enforce_admins.enabled == true) and (.required_pull_request_reviews == null) and (.restrictions == null) and (.allow_force_pushes.enabled == false) and (.allow_deletions.enabled == false)' | grep -qx true''',
    '''set -o pipefail; gh api -H 'X-GitHub-Api-Version: 2026-03-10' repos/NexusAgentX/flowprobe-studio/actions/permissions --jq '(.enabled == true) and (.allowed_actions == "selected") and (.sha_pinning_required == true)' | grep -qx true''',
    '''set -o pipefail; gh api -H 'X-GitHub-Api-Version: 2026-03-10' repos/NexusAgentX/flowprobe-studio/actions/permissions/selected-actions --jq '(.github_owned_allowed == false) and (.verified_allowed == false) and ((.patterns_allowed | sort) == (["actions/checkout@*", "actions/setup-python@*", "actions/setup-node@*", "dtolnay/rust-toolchain@*", "pnpm/action-setup@*"] | sort))' | grep -qx true''',
    '''set -o pipefail; gh api -H 'X-GitHub-Api-Version: 2026-03-10' repos/NexusAgentX/flowprobe-studio/actions/permissions/workflow --jq '(.default_workflow_permissions == "read") and (.can_approve_pull_request_reviews == false)' | grep -qx true''',
    '''set -o pipefail; gh api --paginate --slurp -H 'X-GitHub-Api-Version: 2026-03-10' 'repos/NexusAgentX/flowprobe-studio/rules/branches/main?per_page=100' | jq -e '[add[] | select(.type == "pull_request") | .parameters | select(((.required_approving_review_count // 0) > 0) or (.require_code_owner_review // false) or (.require_last_push_approval // false) or (((.required_reviewers // []) | length) > 0))] | length == 0' | grep -qx true''',
    '''set -o pipefail; gh api -H 'X-GitHub-Api-Version: 2026-03-10' 'repos/NexusAgentX/flowprobe-studio/codeowners/errors?ref=refs%2Fheads%2Fmain' --jq '.errors == []' | grep -qx true''',
]
definition_of_done = ["The only repository changes are mode-100644 blobs specs/tasks/v0.1/GOV-001.toml and docs/development/TASK_CONTRACT.md, and the fail-closed unique-merge-base, exact-path, mode, base-SHA-256, and target-SHA-256 predicate passes", "GOV-001 changes only its goal, six fail-closed remote acceptance elements, and five definition-of-done elements to remove mandatory approvals and strengthen exact live assertions; every other field, order, trust boundary, workflow requirement, negative canary, and release requirement remains byte-for-byte unchanged", "TASK_CONTRACT.md changes only the approval-policy explanation, protection prose and payload, pipefail-enabled full-pagination rules and explicit CODEOWNERS evidence endpoints, positive canary, and final independent-review wording authorized by PLAN-005", "Live classic branch-protection evidence shows strict=true, enforce_admins=true, required_pull_request_reviews absent or null, restrictions=null, allow_force_pushes=false, allow_deletions=false, and exactly contracts, Rust workspace, Desktop renderer, and Task scope (trusted base) bound to GitHub Actions app_id 15368", "Repository Actions remain enabled with allowed_actions=selected and SHA pinning required, the selected-action patterns remain exactly the five checked-in dependencies, workflow-token permissions remain read-only, and Actions cannot approve pull-request reviews", "The .github/CODEOWNERS blob and workflow blobs remain unchanged, the explicit full main-ref CODEOWNERS errors query is empty, the three listed owners remain inside the documented trusted owner/admin boundary, and the evidence explicitly states that retaining CODEOWNERS cannot promise zero GitHub-generated owner notifications", "No active branch ruleset separately requires an approving review, code-owner review, last-push approval, or required reviewer", "A positive canary uses exact GOV-001 identity and an allowed disposable change, records no review_requested or review_request_removed timeline event over its entire lifecycle, has no requested reviewer or submitted review at opening and on both exact head SHAs, is blocked until all four current-SHA app-bound checks succeed on each head, and then becomes eligible without approval after each head", "The body-edit, out-of-scope, same-app same-name spoof, and proposed-head failure canaries still prove fail-closed behavior under the retained machine controls", "All canary pull requests are closed unmerged, their branches are deleted, and no canary commit, file, workflow change, review request, submitted review, or temporary remote state remains", "The user-authorized deletion of the remote required-review protection is represented only by dated pre-change and post-change API evidence and is not misrepresented as a planning-pull-request repository change", "The GOV-003 pull request body contains exactly one unquoted Task ID: GOV-003 line, all current-head checks pass, and an independent technical reviewer reports no P0-P2 issue without creating a GitHub approval or review request", "GOV-001 and v0.1 remain incomplete until the exact merged-main clean-checkout audit, CI, supported-host real proof and cleanup, all API snapshots and canaries, and the independent technical P0-P2-zero review pass"]
