id = "PLAN-004"
milestone = "v0.1"
title = "Authorize explicit-ref CODEOWNERS acceptance repair"
goal = "Authorize one corrective governance task that replaces the externally broken implicit-default-branch CODEOWNERS errors acceptance call with a stricter explicit full-ref query, without weakening GOV-001 or mixing implementation into this planning bootstrap."
depends_on = ["PLAN-003", "INT-003"]
allowed_paths = ["specs/tasks/v0.1/PLAN-004.toml", "specs/tasks/v0.1/GOV-002.toml"]
forbidden_paths = ["AGENTS.md", "PRODUCT.md", "ARCHITECTURE.md", "ROADMAP.md", "docs/**", "specs/tasks/README.md", "specs/tasks/v0.1/GOV-001.toml", "specs/nonconformance/**", "apps/**", "crates/**", "runtime/**", "plugins/**", "tests/**", "scripts/**", ".github/**", "Cargo.toml", "Cargo.lock", "package.json", "pnpm-lock.yaml", "mise.toml"]
acceptance = ["python -B scripts/validate_tasks.py", "python -B scripts/check_task_scope.py check --task PLAN-004 --base-ref origin/main --head-ref HEAD", "python -B scripts/quality_gate.py", "git diff --check origin/main...HEAD"]
definition_of_done = ["PLAN-004 and GOV-002 are the only added paths, both are regular mode-100644 TOML files, and the trusted-base planning bootstrap accepts their merged DAG", "GOV-002 authorizes only the explicit-ref correction to the existing GOV-001 CODEOWNERS errors acceptance command and preserves every other GOV-001 field, acceptance command, and definition-of-done item", "The corrective command binds the audit to the full refs/heads/main identity and asserts that the returned errors array is empty, so it is at least as strict as the documented implicit-default-branch intent", "The observed 404 without ref and successful zero-error response with the explicit full ref are recorded as external live evidence rather than represented as repository implementation state", "This planning pull request changes no existing task contract, workflow, pull-request template, CODEOWNERS file, implementation, documentation, accepted architecture artifact, or remote repository setting", "The planning pull request body contains exactly one unquoted Task ID: PLAN-004 line accepted by the trusted-base parser", "An independent reviewer reports no P0-P2 issue in the corrective scope, dependency order, evidence requirements, or preservation guarantees"]
