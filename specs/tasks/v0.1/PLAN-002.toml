id = "PLAN-002"
milestone = "v0.1"
title = "Encode the v0.1 task-scope nonconformance remediation"
goal = "Turn the release-audit finding for eight historically out-of-scope root-file changes into explicit corrective and final-verification tasks without rewriting the original contracts or claiming that the affected pull requests were compliant when merged."
depends_on = ["INT-002"]
allowed_paths = ["specs/tasks/v0.1/PLAN-002.toml", "specs/tasks/v0.1/SCOPE-001.toml", "specs/tasks/v0.1/INT-003.toml"]
forbidden_paths = ["PRODUCT.md", "ARCHITECTURE.md", "ROADMAP.md", "docs/adr/**", "docs/contracts/**", "docs/milestones/**", "specs/tasks/v0.1/FOUND-001.toml", "specs/tasks/v0.1/FLOW-001.toml", "specs/tasks/v0.1/CFG-001.toml", "specs/tasks/v0.1/CAP-001.toml", "specs/tasks/v0.1/STORE-001.toml", "specs/tasks/v0.1/UI-001.toml", "specs/tasks/v0.1/WASM-001.toml", "specs/tasks/v0.1/NET-001.toml", "specs/tasks/v0.1/INT-001.toml", "specs/tasks/v0.1/PLAN-001.toml", "specs/tasks/v0.1/GATE-001.toml", "specs/tasks/v0.1/TLS-001.toml", "specs/tasks/v0.1/INT-002.toml", "apps/**", "crates/**", "runtime/**", "plugins/**", "tests/**", "scripts/**", ".github/**", "Cargo.toml", "Cargo.lock", "pnpm-lock.yaml", "mise.toml"]
acceptance = ["python -B scripts/validate_tasks.py", "python -B scripts/quality_gate.py", "git diff --check"]
definition_of_done = ["The eight pull requests and fifteen task/path instances that were outside their literal allowed_paths at merge time are represented as historic nonconformance requiring correction, not as a waiver or a claim of original compliance", "SCOPE-001 owns the immutable evidence ledger, explicit adoption of the necessary current root build state, and fail-closed task-scope enforcement", "INT-003 owns the downstream clean-checkout milestone re-verification after SCOPE-001 merges", "The corrective tasks form an acyclic dependency chain after INT-002 and authorize no accepted architecture or contract change", "The thirteen pre-existing v0.1 task contracts and the v0.1 milestone exit criterion remain byte-for-byte unchanged in this planning task", "No implementation, gate, ledger, root manifest, lockfile, milestone claim, or production source is changed in this planning task"]
