id = "INT-002"
milestone = "v0.1"
title = "Close the corrected v0.1 integration and release gate"
goal = "Make the real sing-box proof independent of user curl configuration, integrate the corrected TLS and quality gates, and leave a clean-checkout runbook that an independent verifier can use to decide the v0.1 milestone."
depends_on = ["TLS-001", "GATE-001", "INT-001"]
allowed_paths = ["tests/integration/v0_1/**", "scripts/check_v0_1_integration.py", ".github/workflows/contract.yml", "docs/milestones/v0.1.md"]
forbidden_paths = ["PRODUCT.md", "ARCHITECTURE.md", "docs/adr/**", "docs/contracts/**", "apps/**", "crates/**", "runtime/**", "plugins/**", "tests/fixtures/golden/**"]
acceptance = ["python scripts/validate_tasks.py", "python -m unittest discover -s scripts/tests -p 'test_quality_gate.py'", "python scripts/quality_gate.py", "cargo fmt --all -- --check", "cargo clippy --workspace --all-targets --locked -- -D warnings", "cargo test --workspace --locked", "cargo test --manifest-path tests/integration/v0_1/Cargo.toml --locked --all-targets", "cargo clippy --manifest-path tests/integration/v0_1/Cargo.toml --locked --all-targets -- -D warnings", "python scripts/check_v0_1_integration.py", "pnpm --dir apps/desktop lint", "pnpm --dir apps/desktop test", "mise run check"]
definition_of_done = ["Every proof curl invocation places --disable before every other option so HOME and CURL_HOME configuration cannot weaken certificate or proxy assertions", "A deterministic regression supplies hostile user curl configuration and proves the invocation remains hermetic; removing or moving --disable makes the regression fail", "On a supported verifier host the pinned real sing-box proof, executable-SHA negative test, and supported-platform missing-environment failure are rerun with exact cleanup evidence", "The clean-checkout runbook explicitly distinguishes the generated-CA interception proof from the loopback sing-box CONNECT/direct-egress proof", "Root CI and the isolated integration gate execute the corrected quality and TLS evidence", "An independent verifier rechecks every v0.1 required outcome, no P0-P2 finding remains, and the checkout has no orphan process, temporary proof state, pycache, or tracked diff"]
