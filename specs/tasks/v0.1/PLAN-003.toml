id = "PLAN-003"
milestone = "v0.1"
title = "Authorize v0.1 repository-governance enforcement"
goal = "Add a dedicated governance task that closes the verified GitHub enforcement gap without mixing repository implementation or remote-setting mutation into this planning bootstrap."
depends_on = ["SCOPE-001"]
allowed_paths = ["specs/tasks/v0.1/PLAN-003.toml", "specs/tasks/v0.1/GOV-001.toml"]
forbidden_paths = ["AGENTS.md", "PRODUCT.md", "ARCHITECTURE.md", "ROADMAP.md", "docs/**", "specs/tasks/README.md", "specs/nonconformance/**", "apps/**", "crates/**", "runtime/**", "plugins/**", "tests/**", "scripts/**", ".github/**", "Cargo.toml", "Cargo.lock", "package.json", "pnpm-lock.yaml", "mise.toml"]
acceptance = ["python -B scripts/validate_tasks.py", "python -B scripts/check_task_scope.py check --task PLAN-003 --base-ref origin/main --head-ref HEAD", "python -B scripts/quality_gate.py", "git diff --check"]
definition_of_done = ["PLAN-003 and GOV-001 are the only added paths, both are regular mode-100644 TOML files, and the trusted-base planning bootstrap accepts their merged DAG", "GOV-001 depends on PLAN-003 and INT-003 so repository governance follows the v0.1 re-verification workflow change rather than racing or widening it", "GOV-001 states the explicit trust boundary: current NexusAgentX organization owners and repository administrators are trusted; pull-request code and all other actors are untrusted", "GOV-001 separates repository-file deliverables from post-merge GitHub settings and live canary evidence", "This planning pull request changes no workflow, pull-request template, CODEOWNERS file, implementation, documentation, accepted architecture artifact, existing task contract, or remote repository setting", "The planning pull request body contains exactly one unquoted Task ID: PLAN-003 line accepted by the trusted-base parser", "An independent reviewer reports no P0-P2 issue in the task boundaries, dependency order, threat model, or executable evidence plan"]
