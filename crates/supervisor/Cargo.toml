[package]
name = "flowprobe-supervisor"
description = "Non-renderer lifecycle boundary for FlowProbe Studio"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
flowprobe-ipc = { path = "../ipc" }
flowprobe-model = { path = "../model" }
flowprobe-runtime-api = { path = "../runtime-api" }
flowprobe-storage = { path = "../storage" }

[dev-dependencies]
flowprobe-config-compiler = { path = "../config-compiler" }
serde_json.workspace = true

[lints]
workspace = true
