# v0.7 — Codex Analyzer

Objective: use a real evolving agent product to validate semantic analysis.

Required: controlled discovery fixtures, matcher/confidence model, observable request/stream decoding, turn correlation, timing/request counts, model extraction where observable, exact-vs-estimated token provenance, tool/event timeline where observable, local hourly/daily analytics, graceful degradation when wire formats change.

Exit: analyzer failure never breaks networking/capture, and uncertain or reconstructed metrics are never presented as server-exact values.
