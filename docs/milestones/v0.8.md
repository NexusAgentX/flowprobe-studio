# v0.8 — Analytics & Data Scale

Objective: support long-running local observability efficiently.

Required: Parquet archives, DuckDB query layer, retention tiers, aggregates, analyzer analytics with limits, fast dashboards, CSV/JSON/Parquet export, storage diagnostics/compaction.

Exit: representative multi-week captures remain queryable within documented disk/memory limits without turning SQLite into a raw-payload warehouse.
